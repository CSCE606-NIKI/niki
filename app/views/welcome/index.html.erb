<!DOCTYPE html>
<html>
	<head>
		<title> NIKI </title>
	<head>
	<body>
		<h4> Welcome User </h4>
		<% if current_user %>
    		<%= form_tag logout_path, method: :delete do %>
				<%= submit_tag 'Logout' %>
		    <% end %>
		  
  		<% end %>
	</body>
</html>
